# BalanceLog Pro - Dependencies
# Production-grade balancing record digitization
# NOTE: This build is CPU-only — no GPU/CUDA required.

# UI Framework
PySide6>=6.7.0

# Computer Vision
opencv-python-headless>=4.9.0
numpy>=1.26.0

# OCR Engines
easyocr>=1.7.0
pytesseract>=0.3.10

# PyTorch (CPU-only) — required by EasyOCR for model inference.
# Install CPU-only build to avoid pulling the full GPU version:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# Then install the rest:
#   pip install -r requirements.txt
torch

# Screen Capture
mss>=9.0.0

# Windows API
pywin32>=306

# Data & Export
pandas>=2.2.0
openpyxl>=3.1.0

# PDF Reports
reportlab>=4.1.0

# Image Processing
Pillow>=10.2.0

# Build
pyinstaller>=6.3.0
